{
  "name": "shortcuts-font",
  "repository": "github:fabiospampinato/shortcuts-font",
  "description": "A minimal font designed to beutifully render characters used for representing shortcuts.",
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "clean": "tsex clean",
    "compile": "tsc --skipLibCheck",
    "compile:watch": "tsc --skipLibCheck --watch",
    "font:build": "node dist/build.js",
    "font:extract": "node dist/extract.js",
    "font:update": "npm run font:extract && npm run font:build",
    "prepublishOnly": "npm run clean && npm run compile && npm run font:update"
  },
  "keywords": [
    "shortcuts",
    "font"
  ],
  "devDependencies": {
    "@types/node": "^18.14.0",
    "icon-font-buildr": "^2.0.0",
    "shortcuts": "^2.0.3",
    "tsex": "^2.2.1",
    "typescript": "^4.9.5"
  }
}
